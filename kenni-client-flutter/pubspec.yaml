name: kenni_client_flutter
description: Kenni — Flutter (flutter_appauth) example
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.1"

dependencies:
  flutter:
    sdk: flutter
  flutter_appauth: ^12.0.0
  flutter_secure_storage: ^10.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
